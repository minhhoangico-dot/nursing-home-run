import { supabase } from '../src/lib/supabase';
import { FLOOR_3_RESIDENTS, SAMPLE_SHIFT_NOTES, SAMPLE_BLOOD_SUGAR } from '../src/data/seedData';

// Script to seed the database
// Run using a script runner or importing into the app
export const seedDatabase = async () => {
    // ... same logic as utils/seeder.ts
    console.log('Seed script executed');
}
